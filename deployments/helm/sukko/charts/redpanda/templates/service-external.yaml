{{- if and .Values.enabled .Values.externalAccess.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "redpanda.fullname" . }}-external
  labels:
    {{- include "redpanda.labels" . | nindent 4 }}
  {{- if .Values.externalAccess.annotations }}
  annotations:
    {{- toYaml .Values.externalAccess.annotations | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.externalAccess.type }}
  {{- if .Values.externalAccess.loadBalancerIP }}
  loadBalancerIP: {{ .Values.externalAccess.loadBalancerIP }}
  {{- end }}
  ports:
    - port: {{ .Values.kafkaPort }}
      {{- if .Values.externalAccess.advertisedHost }}
      # When advertisedHost is set, use the external listener port
      targetPort: {{ .Values.externalAccess.containerPort }}
      {{- else }}
      targetPort: kafka
      {{- end }}
      protocol: TCP
      name: kafka
      {{- if and (eq .Values.externalAccess.type "NodePort") .Values.externalAccess.nodePort }}
      nodePort: {{ .Values.externalAccess.nodePort }}
      {{- end }}
  selector:
    {{- include "redpanda.selectorLabels" . | nindent 4 }}
{{- end }}
